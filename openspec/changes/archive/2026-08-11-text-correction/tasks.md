## 1. Подготовка и конфигурация

- [x] 1.1 Добавить `openai` в `backend/requirements.txt` и установить в `.venv`
- [x] 1.2 Добавить константы в `config.py`: `DEEP_CORRECT_MAX_LENGTH = 1000`, `DEFAULT_DEEP_CORRECT = False`, настройки LLM (base_url, model, max_output_tokens)

## 2. LLM-клиент

- [x] 2.1 Переписать `llm_yandex.py`: асинхронный класс на `AsyncOpenAI`, убрать вызов на импорте, инъекция ключа из конфига
- [x] 2.2 Spike: на реальном ключе проверить рабочий endpoint/метод Yandex API, поправить base_url/метод/`max_output_tokens`; зафиксировать промпт «минимальные правки, не менять слова и порядок, вернуть только исправленный текст»

## 3. Алгоритмическая правка

- [x] 3.1 Реализовать исправление опечаток: токенизация `razdel`, проверка `word_is_known`, кандидаты по расстоянию Дамерау-Левенштейна ≤2, замена только при единственном кандидате; не трогать числа, URL, эмодзи, слова с заглавной буквы
- [x] 3.2 Реализовать белый список банвордов: слова из `TARGET_WORDS` и `WORDS_LEMMA` алгоритм не изменяет
- [x] 3.3 Реализовать оформительские правки: пробел после `,.;:!?`, схлопывание двойных пробелов, заглавная буква после точки
- [x] 3.4 Реализовать кэш результатов по хэшу входного текста (in-memory dict)

## 4. Оркестрация коррекции

- [x] 4.1 Реализовать `CorrectionResult` (dataclass): `original_text`, `corrected_text`, `algo_edits`, `deep`, `deep_note`
- [x] 4.2 Реализовать `correct_text(text, deep)`: алгоритм → (если deep и длина ≤ порога) LLM → результат; подсчёт правок алгоритма; ошибка LLM → `deep=False` + `deep_note`

## 5. Интеграция в бот

- [x] 5.1 Реализовать команду `/correct`: переключение глубокой правки на пользователя через `context.user_data`, дефолт из `DEFAULT_DEEP_CORRECT`, подтверждение переключения сообщением
- [x] 5.2 Зарегистрировать `/correct` в `bot.py`
- [x] 5.3 Обновить `handle_text_message`: вызвать коррекцию, ответить исправленным текстом с подсветкой банвордов, статистику отправить отдельным сообщением; сохранить отклонение текста длиннее `MAX_TEXT_LENGTH`
- [x] 5.4 Обработать случаи: нет банвордов (всё равно вернуть исправленный текст), текст длиннее порога глубокой правки (алгоритм + пометка), LLM недоступен (алгоритм + «глубокая правка недоступна»), обновить текст сообщения-индикатора

## 6. Проверка

- [x] 6.1 Ручной тест в Telegram с включённым `/correct`: опечатки, грамматика/пунктуация, банворды-сленг не ломаются, опечатка в банворде исправляется и подсвечивается
- [x] 6.2 Проверить повторную отправку идентичного текста — результат из кэша, без вызова LLM
- [x] 6.3 Проверить недоступность LLM (ключ/сеть) и текст длиннее 1000 символов — алгоритм + пометка о недоступности глубокой правки
